name: Practicum Project Check

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  checklist:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Run checklist
        run: |
          echo "========================================"
          echo "        ПРОВЕРКА ПРОЕКТА ПРАКТИКУМ      "
          echo "========================================"
          
          # 1. Структура проекта
          echo ""
          echo "📁 СТРУКТУРА ПРОЕКТА:"
          [ -f "index.html" ] && echo "✅ index.html" || echo "❌ index.html"
          [ -d "Assets" ] && echo "✅ Папка Assets" || echo "❌ Папка Assets"
          [ -d "Assets/Images" ] && echo "✅ Папка Images" || echo "❌ Папка Images"
          [ -d "Assets/Styles" ] && echo "✅ Папка Styles" || echo "❌ Папка Styles"
          
          # 2. HTML проверки
          echo ""
          echo "📄 HTML ПРОВЕРКИ:"
          if grep -q "<!DOCTYPE html>" index.html; then
            echo "✅ Doctype HTML5"
          else
            echo "❌ Отсутствует doctype"
          fi
          
          if grep -q "viewport" index.html; then
            echo "✅ Viewport meta тег"
          else
            echo "❌ Отсутствует viewport"
          fi
          
          # 3. CSS проверки
          echo ""
          echo "🎨 CSS ПРОВЕРКИ:"
          CSS_FILES=$(find Assets/Styles -name "*.css" | wc -l)
          echo "✅ Найдено CSS файлов: $CSS_FILES"
          
          # 4. Итог
          echo ""
          echo "========================================"
          echo "Проверка завершена! Отправляйте на ревью."
          echo "========================================"